# Chorify AI Chat · 视频复刻交互 Demo

## 当前产品结论

视频复刻不再作为独立“营销工厂页面”，而是 Chorify AI Chat 中可被用户主动调用的结构化工具。

```text
一条 AI 会话
└─ 最多绑定一份视频复刻生产订单
   ├─ 参考视频
   ├─ 目标商品
   ├─ 投放市场与本地化
   ├─ 可选替换对象
   └─ 1 / 3 / 5 条候选视频及版本
```

本版本是独立纯前端 Demo，不修改公司线上 Chorify，也不接入真实 AI、视频生成、产品库、积分、上传、后端任务或导出。

## 已实现主流程

```text
Chorify AI 新会话
→ 从欢迎卡、侧栏工具或输入框调用“视频复刻”
→ 在对话中打开结构化配置表单
→ 表单和自然语言同步更新同一份配置
→ 确认后绑定生产订单并折叠为摘要卡
→ 右侧视频管理立即展示全部候选槽位，并支持卡片/表格切换
→ 候选并行进入等待 / 生成 / 待补充 / 可预览状态
→ 点击任意候选，打开最右视频详情；未完成候选显示真实生产状态
→ 在预览中审核、导出，或把修改要求回流当前 AI 会话
→ 随时重新打开配置，保留候选、选中项、预览和版本历史
```

## 四区职责

- 左侧固定栏：Chorify Chat 模块、工具入口、历史会话和账户。
- 中央对话区：自然语言、结构化表单、进度消息、配置摘要和主动补料问询。
- 右侧视频管理区：首屏直接显示候选，不重复放置批次进度概览；支持卡片/表格双视图。
- 最右视频详情区：任意候选均可打开；未完成候选展示生产状态，已完成候选提供查看、核对、版本、人工审核、导出及修改回流。

右上角任务/预览开关复刻 Frame 的状态语义：打开时紫色、加粗边框和内部线条；关闭时恢复深色细线。

视频复刻表单以 AI 工具消息嵌入原 Chat 对话流。普通消息、工具表单与底部输入框共享最大 `920px` 内容轨道；表单可以使用消息内更大的横向空间，但不得改变 Chat 主栏或输入框宽度。长表单随对话内容区滚动，提交前任务栏和预览栏保持关闭。

## 复刻配置范围

- 参考视频：上传文件或粘贴公开链接。
- 目标商品：从产品库选择或直接上传。
- 投放国家：下拉选择；自动确定语言、字幕、画面文字和口播。
- 商品替换：强制替换原视频中的全部商品露出。
- 人物：人物库、上传照片或 AI 生成。
- 场景：指定原视频中的场景画面进行替换。
- 视频片段：上传片段并指定原镜头/时间段进行替换或二次加工。
- 品牌 Logo：可选替换；普通文字由本地化统一处理。
- 候选数量：1 / 3 / 5 条。
- 高级设置：本阶段只做占位。

## 视觉连续性

- 从欢迎卡、侧栏工具或输入框调用视频复刻后，页面保持原 Chat 的底色与品牌渐变。
- 欢迎态和对话态共用同一套主输入框视觉：白底、边框、18px 圆角、阴影、内边距、宽度和高度均不变化。
- 点击工具只在原对话流中增加消息和表单，不通过换色或输入框变形表达状态切换。

## 验收结果

- 自动测试：`node --test tests/*.test.mjs` 共 52 项通过，0 项失败。
- 浏览器双视图走查：重复批次概览已删除；3 条候选在卡片和表格中一致；点击生成中与已完成候选均打开对应视频详情。
- 浏览器点击前后计算样式比对：画布背景、输入框表面及文本区均一致。
- 浏览器完整走查：调用工具、配置、提交、自动进度、候选选择、预览、修改回流、重新打开配置均通过。
- 1920 × 1080：对话轨道与输入框均为 920px，左右边界一致，工作区无横向溢出。
- 1366 × 768：对话轨道与输入框左右边界一致；长表单可在会话区滚动，主要操作保持可用。
- 浏览器控制台：错误与警告 0。

## 关键文件

- `marketing-factory-model.mjs`：会话绑定订单、配置、候选、版本和面板状态。
- `marketing-factory-view.mjs`：Chat 首页、内嵌表单、候选任务和视频预览。
- `app.js`：事件联动、自动演示节奏和修改回流。
- `styles.css`：桌面端 Chat 与多栏工作区视觉。
- `tests/marketing-factory-model.test.mjs`：复刻订单与候选视图状态测试。
- `tests/marketing-factory-view.test.mjs`：当前 Chat、候选双视图与详情联动测试。
- `docs/superpowers/specs/2026-08-11-chat-video-replication-tool-design.md`：本轮确认的产品设计。
- `docs/superpowers/plans/2026-08-11-chat-video-replication-tool.md`：本轮实施计划。

## 后续边界

- 本版先验证“Chat 调用工具 → 生产候选 → 预览审核”的产品骨架。
- 真实上传、模型参数、任务调度、视频播放器、版本对比、下载和持久化交由正式产品实现。
- 其他 Chat 工具与视频复刻订单之间的跨会话关系仍需后续讨论。
